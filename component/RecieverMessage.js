import React from 'react'
import { View, Text } from 'react-native'

export default function ReceiverMessage({message}) {
    return (
        <View>
            <Text>Reciver message</Text>
        </View>
    )
}
